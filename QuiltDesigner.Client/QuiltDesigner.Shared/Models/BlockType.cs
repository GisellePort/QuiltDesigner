namespace QuiltDesigner.Shared.Models
{
    // This is the type of each quilt block
    // We start with 3 types: Square, Rectangle, Half-Square Triangle (HST)
    public enum BlockType
    {
        Square,
        Rectangle,
        HalfSquareTriangle
    }
}
